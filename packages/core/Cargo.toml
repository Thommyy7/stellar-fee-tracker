[package]
name = "core"
version = "0.1.0"
edition = "2021"
description = "Stellar Fee Tracker core service"
license = "MIT"

[dependencies]
dotenvy = "0.15"
serde = { version = "1.0", features = ["derive"] }
